server:
  servlet:
    encoding:
      charset: utf-8
      force: true
  port: 8080


  logging:
  level:
    com.com.example.projectboard: debug  # ????? ?? ??? ???? ??
    org.springframework.web.servlet: debug  # Spring? ? ??? ?? ??? ???? ??
    org.hibernate.type.descriptor.sql.BasicBinder: trace  # Hibernate? SQL ??? ?? ??? ????? ??

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/teamfresh?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 1234

  jpa:
    open-in-view: true  # ?? ??? ? ???? ???? ?? ??
    defer-datasource-initialization: true  # ???? ????
    hibernate:
      ddl-auto: create  # ?????? ???? ???? ??
      show-sql: true  # ???? SQL ??? ??
      format_sql: true  # SQL ??? ????? ??? ??
      properties:
        '[hibernate.default_batch_fetch_size]': 100
        '[hibernate.format_sql]': true
      output:
        ansi:
          enabled: always

  data:
    rest:
      base-path: /api  # REST API? ?? ?? ??
      detection-strategy: annotated  # ?????? ?? ???? ??

